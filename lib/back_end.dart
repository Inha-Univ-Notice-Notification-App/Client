import 'package:http/http.dart' as http;

class Notice{
  final int day;
  final int heart;
  final int id;
  final int month;
  final String page;
  final String tag;
  final String title;
  final String url;
  final int year;

  Notice(this.day, this.heart, this.id, this.month, this.page, this.tag, this.title, this.url, this.year);
}

